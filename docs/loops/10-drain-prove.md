# Loop 10 — Drain prove

Nothing important vanished. The chrome is quieter.

## Prompt

```
Prove the drain. Do not add features.

1. npm test && npm run check && npm run verify
2. Open the app in a browser. Walk:
   - Create account: four fields visible; Account type closed
   - Second account: waiting names the first admin; Sign out in the body
   - Sign in as admin: Account menu → Sign out still works
   - Board at 900×700: cards on screen; New task is the acid action
   - Open a task: title first; Task details closed
   - Reports: sheet on screen; Display closed; lenses still switch
   - People: list first; Invite closed
3. If a check fails, fix the loop that owns it. Do not re-inflate posters or reopen details.
```

## Verify

- Domain tests, syntax, and Playwright feature file are green.
- You walked the path; a single screenshot is not enough.
