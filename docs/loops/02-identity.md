# Loop 02 — Identity
Implement local sign-up and sign-in, hash passphrases, persist only the session user id, and expose account switching. Verify empty, invalid, and valid form paths before advancing.
