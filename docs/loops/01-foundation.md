# Loop 01 — Foundation
Build the semantic app shell and design tokens, create the IndexedDB stores, seed demo content once, then verify responsive structure and module syntax. Return to this loop until all checks pass.
