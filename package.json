{
  "name": "ledgerlane",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser-smoke.mjs",
    "verify": "node tests/verify-features.mjs",
    "check": "node --check app.mjs && node --check app-core.mjs && node --check import-export.mjs"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
